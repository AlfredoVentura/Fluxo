services:
  - type: web
    name: fluxo-api
    env: docker
    rootDir: backend
    envVars:
      - key: APP_KEY
        generateValue: true
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: DB_CONNECTION
        value: pgsql
      - key: DATABASE_URL
        fromDatabase:
          name: fluxo-db
          property: connectionString

databases:
  - name: fluxo-db
    databaseName: fluxodb
    user: fluxouser
